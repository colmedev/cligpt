{
  "name": "@manzdev/cligpt",
  "version": "1.0.1",
  "description": "Command-line tool that utilizes the GPT (Generative Pre-trained Transformer) language model to return Linux commands that perform the specified action in natural language.",
  "main": "index.mjs",
  "bin": {
    "cligpt": "./index.mjs"
  },
  "scripts": {
    "dev": "node --env-file=.env index.mjs"
  },
  "keywords": [
    "gpt",
    "cli",
    "command-line",
    "linux"
  ],
  "homepage": "https://manz.dev/",
  "author": "ManzDev",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.16.0",
    "ollama": "^0.5.6"
  }
}
